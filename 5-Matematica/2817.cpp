#include <bits/stdc++.h>

int main(){

    int m, n, l;
    char veiculo[1001][1001];
    int soma = 0;

    scanf("%d %d", &m, &n);

    for(int i = 0; i < m; i++){
        for(int j = 0; j < n; j++){
            scanf("%c", &veiculo[i][j]);
            if(veiculo[i][j] == 'C'){
                soma += 
            }
        }
    }
}
